#!/usr/bin/expect -f

#Test client registration

source lib/lib.tcl

client new

register ;#waits for RPL_WELCOME internally

# Check that ISUPPORT is sent
expect_rpl RPL_ISUPPORT

# Check the motd is sent
expect_rpl RPL_MOTDSTART
expect_rpl RPL_MOTD
expect_rpl RPL_ENDOFMOTD

quit
